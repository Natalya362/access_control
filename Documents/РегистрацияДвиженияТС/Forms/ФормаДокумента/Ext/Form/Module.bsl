&НаКлиенте
Процедура ПриОткрытии(Отказ)
    // Устанавливаем текущую дату и время с текущим годом, если поле пустое
    Если Не ЗначениеЗаполнено(Объект.ДатаВремя) Тогда
        ТекущаяДата = ТекущаяДата();
        Объект.ДатаВремя = Дата(Год(ТекущаяДата), Месяц(ТекущаяДата), День(ТекущаяДата), Час(ТекущаяДата), Минута(ТекущаяДата), Секунда(ТекущаяДата));
    КонецЕсли;
КонецПроцедуры  

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
    // Формат отображения даты и времени
    Элементы.ДатаВремя.Формат = "ДЛФ=ДВ";
КонецПроцедуры      

&НаКлиенте
Процедура ДатаВремяПриИзменении(Элемент)
    // Ничего не делаем, так как ДатаВремя содержит полную дату и время
КонецПроцедуры